spring.application.name=trading-strategy-backtester

# Handle API Gateway stage prefix in request path
server.servlet.context-path=/prod

# DynamoDB table name (set by Lambda environment variable in AWS, use default for local)
TABLE_NAME=${TABLE_NAME:BacktestResultsTable}
